name: CI_build

on: [push, pull_request]

jobs:
  build:

    runs-on: windows-2022
    strategy:
      fail-fast: false
      matrix:
        build_configuration: [Release, Debug]
        build_platform: [x64, Win32, ARM64]

    steps:
    - name: Checkout repo
      uses: actions/checkout@v6

    - name: Add msbuild to PATH
      uses: microsoft/setup-msbuild@v3

    - name: MSBuild of plugin dll
      working-directory: vs.proj\
      run: msbuild TCSyslogFinder.vcxproj /m /p:configuration="${{ matrix.build_configuration }}" /p:platform="${{ matrix.build_platform }}"

    - uses: olegtarasov/get-tag@v2.1.4
      id: tagName
      
    - name: zip artifacts for x64
      if: matrix.build_platform == 'x64' && matrix.build_configuration == 'Release'
      run: 7z a TCSyslogFinder_${{ steps.tagName.outputs.tag }}_${{ matrix.build_platform }}.zip .\bin64\*

    - name: zip artifacts for Win32
      if: matrix.build_platform == 'Win32' && matrix.build_configuration == 'Release'
      run: 7z a TCSyslogFinder_${{ steps.tagName.outputs.tag }}_${{ matrix.build_platform }}.zip .\bin\*

    - name: zip artifacts for ARM64
      if: matrix.build_platform == 'ARM64' && matrix.build_configuration == 'Release'
      run: 7z a TCSyslogFinder_${{ steps.tagName.outputs.tag }}_${{ matrix.build_platform }}.zip .\arm64\*

    - name: Archive artifacts for ${{ matrix.build_platform }}
      if: matrix.build_configuration == 'Release'
      uses: actions/upload-artifact@v7
      with:
          name: TCSyslogFinder_${{ steps.tagName.outputs.tag }}_${{ matrix.build_platform }}.zip
          path: TCSyslogFinder_${{ steps.tagName.outputs.tag }}_${{ matrix.build_platform }}.zip

    - name: Create release on tagging
      uses: softprops/action-gh-release@v3
      if: startsWith(github.ref, 'refs/tags/')
      with:
          files: TCSyslogFinder_${{ steps.tagName.outputs.tag }}_${{ matrix.build_platform }}.zip